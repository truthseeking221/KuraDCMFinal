import { LabHistory } from "@/components";

export const labHistoryDemoMeta = {
  id: "labhistory",
  group: "Compositions",
  navLabel: "Lab history",
};

export function LabHistorySection() {
  return <LabHistory />;
}
